[project]
name = "agentic-workspace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=0.3.26",
    "langgraph>=0.5.0",
    "langsmith>=0.4.4",
]
